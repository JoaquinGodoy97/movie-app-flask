{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "yarn start",
            "type": "shell",
            "command": "yarn",
            "args": ["start"],
            "options": {
                "cwd": "${workspaceFolder}/client" // Ensure it's running in the client directory
            },
            "problemMatcher": []
        }
    ]
}